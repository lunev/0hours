{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.ts|*.tsx|*.js|*.jsx) (cd app && npx eslint --fix \"$f\") ;; esac; case \"$f\" in *.ts|*.tsx|*.js|*.jsx|*.json|*.css|*.md) (cd app && npx prettier --write \"$f\") ;; esac; } 2>/dev/null || true",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
